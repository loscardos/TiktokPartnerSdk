# Schema Fixtures

These JSON fixtures are the normalized generator inputs for `TikTokPartnerSdk.Generator`.
They are committed test assets and replace direct generator reads from raw TikTok YAML exports.
Raw docs can still be used out-of-band to regenerate these fixtures, but runtime and package code must not depend on them.
